import { NextPage } from "next";
import { Graph } from "~/components/Graph";
import { NavBar } from "~/components/NavBar";

const Home: NextPage = () => {
  return <Graph />;
};

export default Home;
